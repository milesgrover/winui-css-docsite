import React from "react";
import styles from "./person-picture.module.scss";

export const PersonPicture = (props) => {
    return (
        <div className={styles.person_picture}>
            person picture
        </div>
    );
}